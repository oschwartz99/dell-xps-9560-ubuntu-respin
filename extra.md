### Extra Tips for Using This Repo
- Post install commands:
	- `sudo tlp start`
	- `sudo powertop --auto-tune`
- Use intel graphics:
	- `sudo prime-select intel`
- Use nvidia grpahics:
	- `sudo prime-select nvidia`
